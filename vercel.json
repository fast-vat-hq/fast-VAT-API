{
  "framework": null,
  "installCommand": "npm install && node scripts/download-geolite2.mjs",
  "buildCommand": "npm run build",
  "functions": {
    "api/index.ts": {
      "includeFiles": "{data/**,web/**}"
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
